import java.util.*;
import java.io.*;

class Solution {
    public int solution(String s) {
//         int answer = Integer.parseInt(Character.isDigit(s.charAt(0))?s.substring(0):s.substring(1));
        
//         if(!Character.isDigit(s.charAt(0))&&s.charAt(0)=='-')
//             answer*=-1;
        
        return Integer.parseInt(s);
    }
}